{
  "name": "val_brim",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate-json": "node generate-json.js",
    "dev": "npm run generate-json && vite",
    "build": "npm run generate-json && vite build",
    "preview": "vite preview --port 8080"
  },
  "dependencies": {
    "vue": "^3.3.11",
    "vuetify": "^3.5.3"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.5.2",
    "vite": "^5.0.8"
  }
}
